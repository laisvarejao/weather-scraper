# weather-scraper
A simple HTML scraper that shows the current weather (fahrenheit & celsius) in NYC.

## Install the gem's:

```shell
bundle install
```

## And run with:

```shell
bundle exec rackup
```
